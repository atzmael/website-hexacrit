import styles from "./styles.module.scss";

const NavTouch: React.FunctionComponent<{
    siteTitle?: string;
    current?: string;
    subcurrent?: string;
    page?: string;
    menu: any;
    lang?: string;
}> = ({
    siteTitle,
    current,
    subcurrent,
    page,
    menu,
    lang
}) => {
        return (
            <div>
            </div>
        );
    }

export default NavTouch;